---
title: Lote de pontuação com modelos do R no Azure
description: Execute a pontuação com modelos do R usando o lote do Azure e um conjunto de dados com base na previsão de vendas do varejo repositório em lote.
author: njray
ms.date: 03/29/2019
ms.topic: reference-architecture
ms.service: architecture-center
ms.subservice: reference-architecture
ms.custom: azcat-ai
ms.openlocfilehash: 4fa57168c337b01c8e7d0fc86ba54fee59a7ae47
ms.sourcegitcommit: 1a3cc91530d56731029ea091db1f15d41ac056af
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 04/03/2019
ms.locfileid: "58887914"
---
# <a name="batch-scoring-of-r-machine-learning-models-on-azure"></a><span data-ttu-id="bb731-103">Lote de pontuação de modelos de aprendizado de máquina do R no Azure</span><span class="sxs-lookup"><span data-stu-id="bb731-103">Batch scoring of R machine learning models on Azure</span></span>

<span data-ttu-id="bb731-104">Essa arquitetura de referência mostra como executar o lote de pontuação com modelos do R usando o lote do Azure.</span><span class="sxs-lookup"><span data-stu-id="bb731-104">This reference architecture shows how to perform batch scoring with R models using Azure Batch.</span></span> <span data-ttu-id="bb731-105">O cenário se baseia na previsão de vendas do varejo store, mas essa arquitetura pode ser generalizada para qualquer cenário que exigem a geração de previsões em um grande scaler usando modelos do R.</span><span class="sxs-lookup"><span data-stu-id="bb731-105">The scenario is based on retail store sales forecasting, but this architecture can be generalized for any scenario requiring the generation of predictions on a large scaler using R models.</span></span> <span data-ttu-id="bb731-106">Há uma implantação de referência para essa arquitetura de referência disponível no [GitHub][github].</span><span class="sxs-lookup"><span data-stu-id="bb731-106">A reference implementation for this architecture is available on [GitHub][github].</span></span>

![Diagrama da arquitetura][0]

<span data-ttu-id="bb731-108">**Cenário**: Uma cadeia de supermercado deve prever as vendas de produtos em futuro trimestre.</span><span class="sxs-lookup"><span data-stu-id="bb731-108">**Scenario**: A supermarket chain needs to forecast sales of products over the upcoming quarter.</span></span> <span data-ttu-id="bb731-109">A previsão permite que a empresa a gerenciar melhor o sua cadeia de fornecimento e certifique-se de que ele pode atender à demanda de produtos em cada um de seus repositórios.</span><span class="sxs-lookup"><span data-stu-id="bb731-109">The forecast allows the company to manage its supply chain better and ensure it can meet demand for products at each of its stores.</span></span> <span data-ttu-id="bb731-110">A empresa atualiza suas previsões de cada semana conforme novos dados de vendas da semana anterior se torna disponíveis e o produto a estratégia de marketing para o próximo trimestre é definido.</span><span class="sxs-lookup"><span data-stu-id="bb731-110">The company updates its forecasts every week as new sales data from the previous week becomes available and the product marketing strategy for next quarter is set.</span></span> <span data-ttu-id="bb731-111">Previsões de quantil são geradas para estimar a incerteza das previsões de vendas individuais.</span><span class="sxs-lookup"><span data-stu-id="bb731-111">Quantile forecasts are generated to estimate the uncertainty of the individual sales forecasts.</span></span>

<span data-ttu-id="bb731-112">O processamento envolve as seguintes etapas:</span><span class="sxs-lookup"><span data-stu-id="bb731-112">Processing involves the following steps:</span></span>

1. <span data-ttu-id="bb731-113">Um aplicativo lógico dispara o processo de geração de previsão de uma vez por semana.</span><span class="sxs-lookup"><span data-stu-id="bb731-113">An Azure Logic App triggers the forecast generation process once per week.</span></span>

1. <span data-ttu-id="bb731-114">O aplicativo lógico inicia uma instância de contêiner do Azure executando o Agendador de contêiner do Docker, que dispara os pontuação de trabalhos no cluster em lotes.</span><span class="sxs-lookup"><span data-stu-id="bb731-114">The logic app starts an Azure Container Instance running the scheduler Docker container, which triggers the scoring jobs on the Batch cluster.</span></span>

1. <span data-ttu-id="bb731-115">Pontuação de trabalhos executados em paralelo em todos os nós do cluster em lotes.</span><span class="sxs-lookup"><span data-stu-id="bb731-115">Scoring jobs run in parallel across the nodes of the Batch cluster.</span></span> <span data-ttu-id="bb731-116">Cada nó:</span><span class="sxs-lookup"><span data-stu-id="bb731-116">Each node:</span></span>

    1. <span data-ttu-id="bb731-117">Extrai o trabalhador imagem do Docker do Hub do Docker e inicia um contêiner.</span><span class="sxs-lookup"><span data-stu-id="bb731-117">Pulls the worker Docker image from Docker Hub and starts a container.</span></span>

    1. <span data-ttu-id="bb731-118">Lê dados de entrada e previamente treinados modelos de R do armazenamento de BLOBs do Azure.</span><span class="sxs-lookup"><span data-stu-id="bb731-118">Reads input data and pre-trained R models from Azure Blob storage.</span></span>

    1. <span data-ttu-id="bb731-119">Classifica os dados para produzir as previsões.</span><span class="sxs-lookup"><span data-stu-id="bb731-119">Scores the data to produce the forecasts.</span></span>

    1. <span data-ttu-id="bb731-120">Grava os resultados de previsão no armazenamento de blob.</span><span class="sxs-lookup"><span data-stu-id="bb731-120">Writes the forecast results to blob storage.</span></span>

<span data-ttu-id="bb731-121">A figura abaixo mostra as vendas previstas para quatro produtos (SKUs) em um repositório.</span><span class="sxs-lookup"><span data-stu-id="bb731-121">The figure below shows the forecasted sales for four products (SKUs) in one store.</span></span> <span data-ttu-id="bb731-122">A linha preta é o histórico de vendas, a linha tracejada é a mediana (q50) de previsão, a banda rosa representa os percentuais de 25 e o quinto setenta e faixa azul representa os quinto e o quinto noventa percentis.</span><span class="sxs-lookup"><span data-stu-id="bb731-122">The black line is the sales history, the dashed line is the median (q50) forecast, the pink band represents the twenty-fifth and seventy-fifth percentiles, and the blue band represents the fifth and ninety-fifth percentiles.</span></span>

![Previsões de vendas][1]

## <a name="architecture"></a><span data-ttu-id="bb731-124">Arquitetura</span><span class="sxs-lookup"><span data-stu-id="bb731-124">Architecture</span></span>

<span data-ttu-id="bb731-125">Essa arquitetura é formada pelos componentes a seguir.</span><span class="sxs-lookup"><span data-stu-id="bb731-125">This architecture consists of the following components.</span></span>

<span data-ttu-id="bb731-126">[O Azure Batch] [ batch] é usado para executar trabalhos de geração de previsão em paralelo em um cluster de máquinas virtuais.</span><span class="sxs-lookup"><span data-stu-id="bb731-126">[Azure Batch][batch] is used to run forecast generation jobs in parallel on a cluster of virtual machines.</span></span> <span data-ttu-id="bb731-127">As previsões são feitas usando a máquina previamente treinada implementados em R. o lote do Azure de modelos de aprendizado podem dimensionar automaticamente o número de VMs com base no número de trabalhos enviados para o cluster.</span><span class="sxs-lookup"><span data-stu-id="bb731-127">Predictions are made using pre-trained machine learning models implemented in R. Azure Batch can automatically scale the number of VMs based on the number of jobs submitted to the cluster.</span></span> <span data-ttu-id="bb731-128">Em cada nó, um script R é executado dentro de um contêiner do Docker para pontuar dados e gerar previsões.</span><span class="sxs-lookup"><span data-stu-id="bb731-128">On each node, an R script runs within a Docker container to score data and generate forecasts.</span></span>

<span data-ttu-id="bb731-129">[O armazenamento de BLOBs do Azure] [ blob] é usado para armazenar os dados de entrada, os modelos de aprendizado de máquina previamente treinado e os resultados de previsão.</span><span class="sxs-lookup"><span data-stu-id="bb731-129">[Azure Blob Storage][blob] is used to store the input data, the pre-trained machine learning models, and the forecast results.</span></span> <span data-ttu-id="bb731-130">Ele oferece armazenamento muito econômico para o desempenho que exige essa carga de trabalho.</span><span class="sxs-lookup"><span data-stu-id="bb731-130">It delivers very cost-effective storage for the performance that this workload requires.</span></span>

<span data-ttu-id="bb731-131">[As instâncias de contêiner do Azure] [ aci] fornecem computação sem servidor sob demanda.</span><span class="sxs-lookup"><span data-stu-id="bb731-131">[Azure Container Instances][aci] provide serverless compute on demand.</span></span> <span data-ttu-id="bb731-132">Nesse caso, uma instância de contêiner é implantada em um agendamento para disparar os trabalhos em lotes que geram as previsões.</span><span class="sxs-lookup"><span data-stu-id="bb731-132">In this case, a container instance is deployed on a schedule to trigger the Batch jobs that generate the forecasts.</span></span> <span data-ttu-id="bb731-133">Os trabalhos em lotes são disparados de um script de R usando o [doAzureParallel] [ doAzureParallel] pacote.</span><span class="sxs-lookup"><span data-stu-id="bb731-133">The Batch jobs are triggered from an R script using the [doAzureParallel][doAzureParallel] package.</span></span> <span data-ttu-id="bb731-134">A instância de contêiner será desligado automaticamente depois que os trabalhos tiverem terminado.</span><span class="sxs-lookup"><span data-stu-id="bb731-134">The container instance automatically shuts down once the jobs have finished.</span></span>

<span data-ttu-id="bb731-135">[Aplicativos lógicos do Azure] [ logic-apps] disparar o fluxo de trabalho inteiro ao implantar as instâncias de contêiner em um agendamento.</span><span class="sxs-lookup"><span data-stu-id="bb731-135">[Azure Logic Apps][logic-apps] trigger the entire workflow by deploying the container instances on a schedule.</span></span> <span data-ttu-id="bb731-136">Um conector de instâncias de contêiner do Azure em aplicativos lógicos permite que uma instância a ser implantado em um intervalo de eventos de gatilho.</span><span class="sxs-lookup"><span data-stu-id="bb731-136">An Azure Container Instances connector in Logic Apps allows an instance to be deployed upon a range of trigger events.</span></span>

## <a name="performance-considerations"></a><span data-ttu-id="bb731-137">Considerações sobre o desempenho</span><span class="sxs-lookup"><span data-stu-id="bb731-137">Performance considerations</span></span>

### <a name="containerized-deployment"></a><span data-ttu-id="bb731-138">Implantação em contêineres</span><span class="sxs-lookup"><span data-stu-id="bb731-138">Containerized deployment</span></span>

<span data-ttu-id="bb731-139">Com essa arquitetura, todos os scripts de R executados no [Docker](https://www.docker.com/) contêineres.</span><span class="sxs-lookup"><span data-stu-id="bb731-139">With this architecture, all R scripts run within [Docker](https://www.docker.com/) containers.</span></span> <span data-ttu-id="bb731-140">Isso garante que os scripts executados em um ambiente consistente, com a mesma versão do R e as versões de pacotes, cada vez.</span><span class="sxs-lookup"><span data-stu-id="bb731-140">This ensures that the scripts run in a consistent environment, with the same R version and packages versions, every time.</span></span> <span data-ttu-id="bb731-141">Imagens do Docker separadas são usadas para os contêineres de agendador e de trabalho, porque cada um tem um conjunto diferente de dependências de pacotes de R.</span><span class="sxs-lookup"><span data-stu-id="bb731-141">Separate Docker images are used for the scheduler and worker containers, because each has a different set of R package dependencies.</span></span>

<span data-ttu-id="bb731-142">As instâncias de contêiner do Azure fornece um ambiente sem servidor para executar o contêiner de Agendador.</span><span class="sxs-lookup"><span data-stu-id="bb731-142">Azure Container Instances provides a serverless environment to run the scheduler container.</span></span> <span data-ttu-id="bb731-143">O contêiner de Agendador executa um script R que dispara os trabalhos de pontuação individuais em execução em um cluster de lote do Azure.</span><span class="sxs-lookup"><span data-stu-id="bb731-143">The scheduler container runs an R script that triggers the individual scoring jobs running on an Azure Batch cluster.</span></span>

<span data-ttu-id="bb731-144">Cada nó do cluster em lotes executa o contêiner de trabalho que executa o script de pontuação.</span><span class="sxs-lookup"><span data-stu-id="bb731-144">Each node of the Batch cluster runs the worker container, which executes the scoring script.</span></span>

### <a name="parallelizing-the-workload"></a><span data-ttu-id="bb731-145">Paralelizar a carga de trabalho</span><span class="sxs-lookup"><span data-stu-id="bb731-145">Parallelizing the workload</span></span>

<span data-ttu-id="bb731-146">Quando o lote de pontuação dados com modelos do R, considere como paralelizar a carga de trabalho.</span><span class="sxs-lookup"><span data-stu-id="bb731-146">When batch scoring data with R models, consider how to parallelize the workload.</span></span> <span data-ttu-id="bb731-147">Os dados de entrada devem ser particionados de alguma forma para que a operação de pontuação pode ser distribuída entre os nós do cluster.</span><span class="sxs-lookup"><span data-stu-id="bb731-147">The input data must be partitioned somehow so that the scoring operation can be distributed  across the cluster nodes.</span></span> <span data-ttu-id="bb731-148">Tente diferentes abordagens para descobrir a melhor opção para distribuir sua carga de trabalho.</span><span class="sxs-lookup"><span data-stu-id="bb731-148">Try different approaches to discover the best choice for distributing your workload.</span></span> <span data-ttu-id="bb731-149">Em uma base caso a caso, considere o seguinte:</span><span class="sxs-lookup"><span data-stu-id="bb731-149">On a case-by-case basis, consider the following:</span></span>

- <span data-ttu-id="bb731-150">A quantidade de dados pode ser carregado e processado na memória de um único nó.</span><span class="sxs-lookup"><span data-stu-id="bb731-150">How much data can be loaded and processed in the memory of a single node.</span></span>
- <span data-ttu-id="bb731-151">A sobrecarga de cada trabalho em lotes de inicialização.</span><span class="sxs-lookup"><span data-stu-id="bb731-151">The overhead of starting each batch job.</span></span>
- <span data-ttu-id="bb731-152">A sobrecarga de carregar os modelos de R.</span><span class="sxs-lookup"><span data-stu-id="bb731-152">The overhead of loading the R models.</span></span>

<span data-ttu-id="bb731-153">O cenário usado neste exemplo, os objetos de modelo são grandes e demora apenas alguns segundos para gerar uma previsão de produtos individuais.</span><span class="sxs-lookup"><span data-stu-id="bb731-153">In the scenario used for this example, the model objects are large, and it takes only a few seconds to generate a forecast for individual products.</span></span> <span data-ttu-id="bb731-154">Por esse motivo, você pode agrupar os produtos e executar um único trabalho de lote por nó.</span><span class="sxs-lookup"><span data-stu-id="bb731-154">For this reason, you can group the products and execute a single Batch job per node.</span></span> <span data-ttu-id="bb731-155">Um loop dentro de cada trabalho gera previsões para os produtos em sequência.</span><span class="sxs-lookup"><span data-stu-id="bb731-155">A loop within each job generates forecasts for the products sequentially.</span></span> <span data-ttu-id="bb731-156">Esse método acaba sendo a maneira mais eficiente paralelizar essa carga de trabalho específica.</span><span class="sxs-lookup"><span data-stu-id="bb731-156">This method turns out to be the most efficient way to parallelize this particular workload.</span></span> <span data-ttu-id="bb731-157">Isso evita a sobrecarga de iniciar vários trabalhos em lotes menores e repetidamente ao carregar os modelos de R.</span><span class="sxs-lookup"><span data-stu-id="bb731-157">It avoids the overhead of starting many smaller Batch jobs and repeatedly loading the R models.</span></span>

<span data-ttu-id="bb731-158">Uma abordagem alternativa é disparar um trabalho de lote por produto.</span><span class="sxs-lookup"><span data-stu-id="bb731-158">An alternative approach is to trigger one Batch job per product.</span></span> <span data-ttu-id="bb731-159">O lote do Azure automaticamente uma fila de trabalhos de formulários e enviá-los a ser executado no cluster como nós se tornam disponíveis.</span><span class="sxs-lookup"><span data-stu-id="bb731-159">Azure Batch automatically forms a queue of jobs and submits them to be executed on the cluster as nodes become available.</span></span> <span data-ttu-id="bb731-160">Use [dimensionamento automático] [ autoscale] para ajustar o número de nós no cluster, dependendo do número de trabalhos.</span><span class="sxs-lookup"><span data-stu-id="bb731-160">Use [automatic scaling][autoscale] to adjust the number of nodes in the cluster depending on the number of jobs.</span></span> <span data-ttu-id="bb731-161">Essa abordagem faz mais sentido se demorar um tempo relativamente longo para concluir cada operação de pontuação, justificar a sobrecarga de iniciar os trabalhos e recarregar os objetos de modelo.</span><span class="sxs-lookup"><span data-stu-id="bb731-161">This approach makes more sense if it takes a relatively long time to complete each scoring operation, justifying the overhead of starting the jobs and reloading the model objects.</span></span> <span data-ttu-id="bb731-162">Essa abordagem também é mais simples de implementar e oferece a flexibilidade para usar o dimensionamento automático — uma consideração importante se o tamanho da carga de trabalho total não é conhecido de antemão.</span><span class="sxs-lookup"><span data-stu-id="bb731-162">This approach is also simpler to implement and gives you the flexibility to use automatic scaling—an important consideration if the size of the total workload is not known in advance.</span></span>

## <a name="monitoring-and-logging-considerations"></a><span data-ttu-id="bb731-163">Considerações de monitoramento e registro em log</span><span class="sxs-lookup"><span data-stu-id="bb731-163">Monitoring and logging considerations</span></span>

### <a name="monitoring-azure-batch-jobs"></a><span data-ttu-id="bb731-164">Monitorando trabalhos de lote do Azure</span><span class="sxs-lookup"><span data-stu-id="bb731-164">Monitoring Azure Batch jobs</span></span>

<span data-ttu-id="bb731-165">Monitorar e encerrar trabalhos em lotes do **trabalhos** painel da conta do lote no portal do Azure.</span><span class="sxs-lookup"><span data-stu-id="bb731-165">Monitor and terminate Batch jobs from the **Jobs** pane of the Batch account in the Azure portal.</span></span> <span data-ttu-id="bb731-166">Monitorar o cluster de lote, incluindo o estado de nós individuais, do **Pools** painel.</span><span class="sxs-lookup"><span data-stu-id="bb731-166">Monitor the batch cluster, including the state of individual nodes, from the **Pools** pane.</span></span>

### <a name="logging-with-doazureparallel"></a><span data-ttu-id="bb731-167">Registro em log com doAzureParallel</span><span class="sxs-lookup"><span data-stu-id="bb731-167">Logging with doAzureParallel</span></span>

<span data-ttu-id="bb731-168">O pacote doAzureParallel automaticamente coleta logs de todos os stdout/stderr para todos os trabalhos enviados no lote do Azure.</span><span class="sxs-lookup"><span data-stu-id="bb731-168">The doAzureParallel package automatically collects logs of all stdout/stderr for every job submitted on Azure Batch.</span></span> <span data-ttu-id="bb731-169">Eles podem ser encontrados na conta de armazenamento criada na instalação.</span><span class="sxs-lookup"><span data-stu-id="bb731-169">These can be found in the storage account created at setup.</span></span> <span data-ttu-id="bb731-170">Para exibi-los, use uma ferramenta de navegação de armazenamento, como [Azure Storage Explorer] [ storage-explorer] ou portal do Azure.</span><span class="sxs-lookup"><span data-stu-id="bb731-170">To view them, use a storage navigation tool such as [Azure Storage Explorer][storage-explorer] or Azure portal.</span></span>

<span data-ttu-id="bb731-171">Para depurar rapidamente os trabalhos em lote durante o desenvolvimento, imprimir os logs em sua sessão R local usando o [getJobFiles] [ getJobFiles] função de doAzureParallel.</span><span class="sxs-lookup"><span data-stu-id="bb731-171">To quickly debug Batch jobs during development, print logs in your local R session using the [getJobFiles][getJobFiles] function of doAzureParallel.</span></span>

## <a name="cost-considerations"></a><span data-ttu-id="bb731-172">Considerações de custo</span><span class="sxs-lookup"><span data-stu-id="bb731-172">Cost considerations</span></span>

<span data-ttu-id="bb731-173">Os recursos de computação usados nesta arquitetura de referência são os componentes de custo mais alto.</span><span class="sxs-lookup"><span data-stu-id="bb731-173">The compute resources used in this reference architecture are the most costly components.</span></span> <span data-ttu-id="bb731-174">Para este cenário, um cluster de tamanho fixo é criado sempre que o trabalho é disparado e, em seguida, desligado após a conclusão do trabalho.</span><span class="sxs-lookup"><span data-stu-id="bb731-174">For this scenario, a cluster of fixed size is created whenever the job is triggered and then shut down after the job has completed.</span></span> <span data-ttu-id="bb731-175">O custo é incorrido apenas enquanto os nós de cluster estão iniciando, executando ou desligar.</span><span class="sxs-lookup"><span data-stu-id="bb731-175">Cost is incurred only while the cluster nodes are starting, running, or shutting down.</span></span> <span data-ttu-id="bb731-176">Essa abordagem é adequada para um cenário em que os recursos de computação necessários para gerar as previsões permanecem relativamente constantes de um trabalho para o trabalho.</span><span class="sxs-lookup"><span data-stu-id="bb731-176">This approach is suitable for a scenario where the compute resources required to generate the forecasts remain relatively constant from job to job.</span></span>

<span data-ttu-id="bb731-177">Em cenários em que a quantidade de computação necessária para concluir o trabalho não é conhecida de antemão, pode ser mais adequado ao usar o dimensionamento automático.</span><span class="sxs-lookup"><span data-stu-id="bb731-177">In scenarios where the amount of compute required to complete the job is not known in advance, it may be more suitable to use automatic scaling.</span></span> <span data-ttu-id="bb731-178">Com essa abordagem, o tamanho do cluster é dimensionado para cima ou para baixo, dependendo do tamanho do trabalho.</span><span class="sxs-lookup"><span data-stu-id="bb731-178">With this approach, the size of the cluster is scaled up or down depending on the size of the job.</span></span> <span data-ttu-id="bb731-179">O lote do Azure dá suporte a uma variedade de fórmula de dimensionamento automático que você pode definir ao definir o cluster usando o [doAzureParallel] [ doAzureParallel] API.</span><span class="sxs-lookup"><span data-stu-id="bb731-179">Azure Batch supports a range of auto-scale formulae which you can set when defining the cluster using the [doAzureParallel][doAzureParallel] API.</span></span>

<span data-ttu-id="bb731-180">Para alguns cenários, o tempo entre trabalhos pode ser muito curto para desligar e iniciar o cluster.</span><span class="sxs-lookup"><span data-stu-id="bb731-180">For some scenarios, the time between jobs may be too short to shut down and start up the cluster.</span></span> <span data-ttu-id="bb731-181">Nesses casos, manter o cluster em execução entre trabalhos, se apropriado.</span><span class="sxs-lookup"><span data-stu-id="bb731-181">In these cases, keep the cluster running between jobs if appropriate.</span></span>

<span data-ttu-id="bb731-182">DoAzureParallel e lote do Azure suportam o uso de VMs de baixa prioridade.</span><span class="sxs-lookup"><span data-stu-id="bb731-182">Azure Batch and doAzureParallel support the use of low-priority VMs.</span></span> <span data-ttu-id="bb731-183">Essas VMs são fornecidos com um desconto significativo, mas a riscos que está sendo apropriados por outras cargas de trabalho de prioridade mais alta.</span><span class="sxs-lookup"><span data-stu-id="bb731-183">These VMs come with a significant discount but risk being appropriated by other higher priority workloads.</span></span> <span data-ttu-id="bb731-184">Portanto, o uso dessas VMs não são recomendados para cargas de trabalho de produção críticos.</span><span class="sxs-lookup"><span data-stu-id="bb731-184">The use of these VMs are therefore not recommended for critical production workloads.</span></span> <span data-ttu-id="bb731-185">No entanto, eles são muito úteis para experimentais ou cargas de trabalho de desenvolvimento.</span><span class="sxs-lookup"><span data-stu-id="bb731-185">However, they are very useful for experimental or development workloads.</span></span>

## <a name="deployment"></a><span data-ttu-id="bb731-186">Implantação</span><span class="sxs-lookup"><span data-stu-id="bb731-186">Deployment</span></span>

<span data-ttu-id="bb731-187">Para implantar essa arquitetura de referência, siga as etapas descritas a [GitHub] [ github] repositório.</span><span class="sxs-lookup"><span data-stu-id="bb731-187">To deploy this reference architecture, follow the steps described in the [GitHub][github] repo.</span></span>


[0]: ./_images/batch-scoring-r-models.png
[1]: ./_images/sales-forecasts.png
[aci]: /azure/container-instances/container-instances-overview
[autoscale]: /azure/batch/batch-automatic-scaling
[batch]: /azure/batch/batch-technical-overview
[blob]: /azure/storage/blobs/storage-blobs-introduction
[doAzureParallel]: https://github.com/Azure/doAzureParallel/blob/master/docs/32-autoscale.md
[getJobFiles]: /azure/machine-learning/service/how-to-train-ml-models
[github]: https://github.com/Azure/RBatchScoring
[logic-apps]: /azure/logic-apps/logic-apps-overview
[storage-explorer]: /azure/vs-azure-tools-storage-manage-with-storage-explorer?tabs=windows